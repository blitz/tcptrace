fix Linux compatibility (always done, always need to be fixed :-(  )
write a "catxpl" command that can combine xpl plots into one
make -i work like -o (accept files)
fix my implementation of inet_pton - doesn't support 1234::6789
add ATM tcpdump support
